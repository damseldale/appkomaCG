export const getBounds = (object) => { const t = object.transform; const width = (object.width || 40) * (t.scaleX || 1); const height = (object.height || 40) * (t.scaleY || 1); return { left: t.x, top: t.y, right: t.x + width, bottom: t.y + height, centerX: t.x + width / 2, centerY: t.y + height / 2, width, height }; };
export const getAlignmentGuides = (objects, movingId, threshold = 8) => { const moving = objects[movingId]; if (!moving) return { x: null, y: null }; const a = getBounds(moving); let x = null; let y = null; Object.values(objects).forEach((candidate) => { if (candidate.id === movingId || !candidate.visible) return; const b = getBounds(candidate); const xs = [[a.left,b.left],[a.centerX,b.centerX],[a.right,b.right],[a.left,b.right],[a.right,b.left]]; const ys = [[a.top,b.top],[a.centerY,b.centerY],[a.bottom,b.bottom],[a.top,b.bottom],[a.bottom,b.top]]; if (x === null) { const hit = xs.find(([u,v]) => Math.abs(u-v)<=threshold); if(hit) x = hit[1]-hit[0]; } if (y === null) { const hit = ys.find(([u,v]) => Math.abs(u-v)<=threshold); if(hit) y = hit[1]-hit[0]; } }); return { x, y }; };
